# SPDX-FileCopyrightText: Elio Severo Junior <elioseverojunior@gmail.com>
#
# SPDX-License-Identifier: MIT OR Apache-2.0

name: CICD

concurrency:
  group: cicd-${{ github.ref_name }}
  cancel-in-progress: ${{ github.ref_name != github.event.repository.default_branch }}

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      dry-run:
        description: "Perform a dry run without publishing"
        required: false
        type: boolean
        default: false

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  # `mise run` installs [tools] before every task, which overrides mise-action's
  # `install: false`: the Supply Chain job spent 8m57s having `mise run comply`
  # build all 26 pinned tools from source, because mise looks for cargo-binstall
  # at ~/.cargo/bin and CI has none, so its cargo backend compiles instead. The
  # gates below use mise for its config flags, not for provisioning --
  # setup-rust-env has already put every tool they need on PATH as a prebuilt
  # binary.
  MISE_TASK_RUN_AUTO_INSTALL: false
  # mise.toml's [env] does `GITHUB_TOKEN = {{env.GITHUB_TOKEN | default(value='')}}`,
  # so with nothing set it exports an EMPTY token -- worse than unset, since any
  # tool that reads it then makes unauthenticated, rate-limited API calls.
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN || github.token }}
  GITVERSION_SPEC: ${{ vars.GITVERSION_SPEC || '6.x' }}
  REF: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.ref }}

jobs:
  # ──────────────────────────────────────────────
  #  1.  Setup — shared bootstrap for all jobs
  # ──────────────────────────────────────────────
  setup:
    name: Setup
    runs-on: ${{ vars.RUNS_ON_ARM64 || vars.RUNS_ON || 'ubuntu-latest' }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: "recursive"
          fetch-depth: 0
          fetch-tags: "true"
          ref: ${{ env.REF }}

      - name: Setup Environment
        uses: ./.github/actions/setup-rust-env

  # ──────────────────────────────────────────────
  #  2.  Version - shared GitVersion SemVer
  # ──────────────────────────────────────────────
  # GitVersion runs HERE and nowhere else in this workflow. It used to be three
  # steps inside `release` -- cache, setup, execute -- which meant the tool was
  # provisioned and the version recomputed inside a job that also tags, pushes
  # and publishes. version.yml owns that now, so there is one definition of what
  # the version is and one place that derives `majorMinor`, which the action
  # itself does not publish.
  #
  # A ROOT job -- no `needs`, no `if:`. It derives the version from tags and
  # commit distance, so it wants the checkout and nothing else; waiting on `build`
  # would serialise it behind the whole test-and-build chain for no reason.
  #
  # The absence of an `if:` is load-bearing, not an oversight. `build` depends on
  # this job and `build` runs on pull requests, and GitHub skips the dependents of
  # a skipped job -- so gating this on `push` to the default branch would skip
  # `build` on every pull request. A job that only `release` consumed could be
  # conditional; one that `build` consumes cannot.
  version:
    name: Version
    permissions:
      contents: read
    uses: ./.github/workflows/version.yml

  # ──────────────────────────────────────────────
  #  3.  Lint — HK project lints
  # ──────────────────────────────────────────────
  lint:
    name: Lint
    # `setup` and `version` are the two roots and run in parallel; everything
    # downstream gates on both. Naming `version` in this first tier is what makes
    # a version failure stop the run here rather than surfacing 20 minutes later
    # in `build` -- and `tests` inherits the dependency transitively, so it does
    # not need repeating there.
    needs: [setup, version]
    runs-on: ${{ vars.RUNS_ON_ARM64 || vars.RUNS_ON || 'ubuntu-latest' }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: "recursive"
          fetch-depth: 0
          fetch-tags: "true"
          ref: ${{ env.REF }}

      - name: Setup Environment
        uses: ./.github/actions/setup-rust-env

      - name: Run HK Lints
        run: hk check --all

  # ──────────────────────────────────────────────
  #  4. SAST — CodeQL static analysis
  # ──────────────────────────────────────────────
  # No `runs-on` here, and its absence is required rather than an oversight. A
  # job that calls a reusable workflow with `uses:` may set only `name`, `uses`,
  # `with`, `secrets`, `needs`, `if` and `permissions`; GitHub rejects the file
  # at parse time otherwise -- `Invalid workflow file ... Unexpected value
  # 'runs-on'` -- which takes down every job in this workflow, not just this one.
  #
  # No runner steering is lost. codeql.yml's own job carries the identical
  # `vars.RUNS_ON_ARM64 || vars.RUNS_ON || 'ubuntu-latest'` ladder, and the
  # runner is chosen by the job that actually executes, so enabling self-hosted
  # arm64 moves this analysis with everything else. What this forgoes is giving
  # SAST a runner DIFFERENT from codeql.yml's other callers; that needs a
  # `runs-on` input on the callee passed through `with:`, not a key here.
  sast:
    name: SAST
    needs: [setup, version]
    permissions:
      security-events: write
      actions: read
      contents: read
    uses: ./.github/workflows/codeql.yml

  # ──────────────────────────────────────────────
  #  5.  MSRV — compile at the DECLARED minimum
  # ──────────────────────────────────────────────
  # The job that keeps `rust-version` honest. Without it an MSRV is a claim, not
  # a constraint: every other job here builds with rust-toolchain.toml's pin, so
  # the declared floor can only ever drift UPWARD, and always silently -- the
  # pinned toolchain never disagrees with it. That is exactly how the workspace
  # came to advertise 1.97 while the code still compiled on 1.88, making every
  # routine toolchain bump an invisible breaking change for consumers (issue #3).
  msrv:
    name: MSRV
    # First tier, alongside `lint`/`sast`, rather than downstream of them: this
    # is one `cargo check`, and a broken floor should surface in three minutes
    # instead of behind the full test-and-build chain.
    needs: [setup, version, lint, sast]
    runs-on: ${{ vars.RUNS_ON_ARM64 || vars.RUNS_ON || 'ubuntu-latest' }}
    permissions:
      contents: read
    if: >
      success()
      && needs.setup.result == 'success'
      && needs.version.result == 'success'
      && needs.lint.result == 'success'
      && needs.sast.result == 'success'
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: "recursive"
          fetch-depth: 0
          fetch-tags: "true"
          ref: ${{ env.REF }}

      # READ the floor; never hardcode it. A literal version here would be a
      # second source of truth free to disagree with Cargo.toml -- the same class
      # of bug this job exists to catch, just relocated into the workflow.
      - name: Read declared MSRV from Cargo.toml
        id: msrv
        run: |
          set -euo pipefail
          version="$(sed -n 's/^rust-version[[:space:]]*=[[:space:]]*"\([^"]*\)".*/\1/p' Cargo.toml | head -n1)"
          if [ -z "$version" ]; then
            echo "::error file=Cargo.toml::no rust-version found in [workspace.package]"
            exit 1
          fi

          # Two files declare the floor to two different tools, and they have to
          # agree. Cargo.toml's `rust-version` is the promise to consumers and
          # what this job compiles against; .clippy.toml's `msrv` is what makes
          # clippy REJECT an API newer than the floor even though the pinned
          # toolchain offers it -- a check that catches at lint time what this
          # job only catches once the code is written.
          #
          # Nothing else compares them. Left unchecked, .clippy.toml silently
          # keeps linting against a floor the project abandoned, which is the
          # exact defect this job exists to prevent, one file over. A missing
          # `msrv` is failed too, not tolerated: dropping the key would quietly
          # switch clippy back to assuming the pinned toolchain.
          clippy_msrv="$(sed -n 's/^msrv[[:space:]]*=[[:space:]]*"\([^"]*\)".*/\1/p' .clippy.toml | head -n1)"
          if [ -z "$clippy_msrv" ]; then
            echo "::error file=.clippy.toml::no msrv key; it must declare msrv = \"${version}\" to match Cargo.toml"
            exit 1
          fi
          if [ "$clippy_msrv" != "$version" ]; then
            echo "::error file=.clippy.toml::msrv \"${clippy_msrv}\" disagrees with Cargo.toml rust-version \"${version}\""
            exit 1
          fi

          echo "version=${version}" >> "$GITHUB_OUTPUT"
          echo "Declared MSRV: ${version} (Cargo.toml and .clippy.toml agree)"

      # Deliberately NOT ./.github/actions/setup-rust-env, which every other job
      # here uses. That action provisions the DEVELOPMENT environment -- mold,
      # clippy/rustfmt/llvm-tools, cargo-binstall, every mise `cargo:` tool,
      # comply and ignorefile -- and it builds that tooling with whatever
      # toolchain is active. Pointing it at the MSRV therefore tried to compile
      # the tooling with 1.88, and it died on the first tool that had moved on:
      #
      #   error: cannot install package `ignorefile-cli 0.1.0`, it requires
      #   rustc 1.95 or newer, while the currently active rustc version is 1.88.0
      #
      # None of that tooling is part of the promise made to consumers, and a gate
      # that fails because a lint helper wants a newer compiler reports nothing
      # about the MSRV. So this job installs rustc and cargo at the declared
      # floor and nothing else. rustup ships preinstalled on GitHub runners.
      #
      # Dropping the composite drops mold with it, which is why the step below
      # must name RUSTFLAGS. See there.
      #
      # No target cache either: the gate's worth is its reliability, not its
      # speed, it runs in the first tier in parallel, and `tests`/`build` set the
      # critical path regardless. Add one only if this ever becomes the slow job.
      - name: Install the declared MSRV toolchain
        env:
          MSRV: ${{ steps.msrv.outputs.version }}
        run: |
          set -euo pipefail
          rustup toolchain install "$MSRV" --profile minimal --no-self-update
          rustup run "$MSRV" rustc --version

      - name: Compile every publishable crate at the MSRV
        env:
          # Selects the toolchain installed above for every cargo/rustc call in
          # this step; it outranks rust-toolchain.toml. This is the override that
          # makes the job mean anything.
          RUSTUP_TOOLCHAIN: ${{ steps.msrv.outputs.version }}
          # REQUIRED, not tidiness. .cargo/config.toml's
          # `[target.x86_64-unknown-linux-gnu]` sets
          # `["-D", "warnings", "-C", "link-arg=-fuse-ld=mold"]`, and mold is
          # installed by setup-rust-env -- which this job deliberately does not
          # run. A RUSTFLAGS env var REPLACES that list rather than merging into
          # it, so naming the flag here keeps the warning policy byte-for-byte
          # while dropping a linker that is not on this runner.
          #
          # `cargo check` does NOT get a pass on that: it still builds and RUNS
          # build scripts, and still compiles proc-macro crates, both of which
          # link. This graph has ~30 of them (serde_derive, proc-macro2, quote,
          # wasm-bindgen-macro, ...). Delete this line and the job fails at the
          # first one, for a reason that has nothing to do with the MSRV.
          #
          # No `--target` is passed, so RUSTFLAGS reaches those host artifacts
          # too, which is exactly what makes the override work.
          #
          # Warnings stay denied AT THE FLOOR because `lint` cannot cover this:
          # it runs on the pinned toolchain, so a lint that fires on 1.88 but was
          # renamed or removed by the pin would go unseen. Verified clean -- the
          # check below passes with zero warnings on 1.88.
          #
          # The trade-off to watch as the floor and stable drift apart: an
          # `#[allow]` naming a lint 1.88 has never heard of raises
          # `unknown_lints`, which would fail a gate that blocks `release` for a
          # reason that says nothing about MSRV. If that day comes, set this to
          # "" rather than weakening the compile check -- `lint` still owns the
          # warning gate on the pinned toolchain.
          RUSTFLAGS: "-D warnings"
        run: |
          set -euo pipefail
          # Derive the crate list instead of maintaining `--exclude` flags:
          # cargo reports `publish` as [] for `publish = false` and null when
          # publishing is allowed, so this selects exactly what a consumer can
          # install from crates.io -- and a crate added tomorrow is covered the
          # day it lands rather than escaping the gate until someone notices.
          #
          # bench/fuzz/yaml-test-suite are excluded by that rule, which is the
          # point: dev-only crates drag in criterion, tarpaulin and a nightly
          # requirement, and their floors are not part of the promise made to
          # consumers. For the same reason this is `check`, not `test` -- running
          # the suite would let a dev-dependency dictate the published MSRV.
          mapfile -t names < <(
            cargo metadata --format-version 1 --no-deps \
              | jq -r '.packages[] | select(.publish != []) | .name' | sort
          )
          if [ "${#names[@]}" -eq 0 ]; then
            echo "::error::no publishable crates resolved; refusing to pass vacuously"
            exit 1
          fi
          packages=()
          for name in "${names[@]}"; do packages+=(--package "${name}"); done
          echo "MSRV $(rustc --version)"
          printf 'Checking %s\n' "${names[@]}"
          # `--all-features` because the floor has to hold for every feature a
          # consumer can switch on; a default-only check would let an optional
          # dependency raise the real MSRV without ever failing here.
          cargo check --locked --all-features "${packages[@]}"

  # ──────────────────────────────────────────────
  #  6. Test
  # ──────────────────────────────────────────────
  # Tests and coverage are one job because they are one command: tarpaulin
  # builds and runs the suite to instrument it, so a separate `mise run test`
  # step compiled and ran everything twice. It also let the two disagree --
  # tests passed in `Format, Lint, Test` while this job failed on the coverage
  # the very same run produced.
  tests:
    name: Tests
    needs: [setup, version, lint, sast]
    runs-on: ${{ vars.RUNS_ON_ARM64 || vars.RUNS_ON || 'ubuntu-latest' }}
    permissions:
      contents: read
    if: >
      success()
      && needs.setup.result == 'success'
      && needs.version.result == 'success'
      && needs.lint.result == 'success'
      && needs.sast.result == 'success'
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: "recursive"
          fetch-depth: 0
          fetch-tags: "true"
          ref: ${{ env.REF }}
          token: ${{ secrets.GITHUB_TOKEN || github.token }}

      # llvm-tools, which tarpaulin's llvm engine needs to instrument doctests,
      # comes from rust-toolchain.toml's `components`.
      # setup-rust-env runs jdx/mise-action itself, keyed on mise.lock/mise.toml.
      # A second invocation used to follow this one with
      # `cache_key: mise-${{ steps.rust-env.outputs.cache-key }}` -- but that
      # output is the CARGO key, hashed on Cargo.lock, so it cached the mise
      # toolset under a key that changes whenever a Rust dependency moves. Two
      # caches for one thing, the second invalidated by input it does not depend
      # on. One setup, keyed on the file that actually describes it.
      - uses: ./.github/actions/setup-rust-env

      # The nightly install that used to sit here is gone, and its absence is the
      # fix rather than a tidy-up. It installed the FLOATING `nightly` channel,
      # which is what tarpaulin then instrumented doctests with while unit tests
      # used the stable pin -- two compilers feeding one profraw merge. The
      # 2026-08-08 nightly changed the profraw format and the merge stopped
      # parsing, turning a toolchain the project never chose into the reason CI
      # was red.
      #
      # `mise run coverage` now installs and selects a DATED nightly itself, so
      # the pin lives in mise.toml beside the tarpaulin invocation it governs --
      # one source of truth that local runs and CI share, rather than a version
      # named here and a command that uses it there. It keeps the serialised
      # up-front rustup call that this step existed to provide (see mise.toml for
      # the race it avoids).
      - name: Run tests with coverage
        env:
          RUST_COVERAGE_THRESHOLD: ${{ vars.RUST_COVERAGE_THRESHOLD || '100' }}
        run: mise run coverage

      # `always()` means this runs even when coverage failed, where lcov.info was
      # never written -- the bare command then failed too and buried the real
      # error under "opening LCOV file: No such file or directory".
      - name: Convert LCOV to SARIF
        if: always()
        run: |
          if [ -s lcov.info ]; then
            cargo-crap --lcov lcov.info --format sarif > results.sarif
          else
            echo "No lcov.info: coverage did not complete. Skipping SARIF conversion."
          fi

      # Uploads unless a runner declares it cannot. GitHub sets no such variable,
      # so this always runs there; the escape hatch exists for `act`, whose
      # artifact server rejects a field upload-artifact v7 sends and fails a run
      # that otherwise passed. Setting `UPLOAD_ARTIFACTS=false` in act's vars
      # skips the step. The source repository generated that file from a
      # `mise run act:setup` task, which is deliberately NOT ported: this
      # repository has no act configuration at all (no .actrc, no .act/), so the
      # task would be machinery with nothing to configure. Add both together if
      # local act runs are ever wanted here.
      - name: Upload coverage artifact
        if: ${{ always() && vars.UPLOAD_ARTIFACTS != 'false' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-report
          if-no-files-found: error
          path: |
            cobertura.xml
            lcov.info
            results.sarif

      - name: Generate Summary Report
        if: always()
        uses: ./.github/actions/summary
        with:
          file-path: results.sarif
          title: "Coverage Report"
          tool-name: "Tarpaulin"

  # ──────────────────────────────────────────────
  #  7.  Build
  # ──────────────────────────────────────────────
  build:
    name: Build
    # `version` as well as `tests`: a job can only read `needs.<job>.outputs` from
    # a DIRECT dependency, so anything here that stamps the version has to name it
    # even though `release` does too.
    # `setup`/`version` only. `build` consumes nothing from `tests` -- no
    # artifact passes between them -- so waiting on it served no purpose beyond
    # serialising the two longest jobs. `release` below now depends on both, so
    # the gate is unchanged: a failing test still blocks the release.
    needs: [setup, version, lint, sast]
    if: >
      success()
      && needs.setup.result == 'success'
      && needs.version.result == 'success'
      && needs.lint.result == 'success'
      && needs.sast.result == 'success'
    runs-on: ${{ vars.RUNS_ON_ARM64 || vars.RUNS_ON || 'ubuntu-latest' }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: "recursive"
          fetch-depth: 0
          fetch-tags: "true"
          ref: ${{ env.REF }}

      - name: Setup Environment
        uses: ./.github/actions/setup-rust-env

      # `--locked` because this is the artefact the release job gates on: a build
      # that silently resolved a different dependency set than Cargo.lock pins is
      # not the thing that was tested.
      - name: Build release binary
        shell: bash -l {0}
        run: |
          set -eo pipefail

          # `--all-features` is load-bearing rather than belt-and-braces, and
          # the reason is a disagreement between two views of the same
          # workspace. `cargo metadata` reports EVERY bin target regardless of
          # feature gating, and the smoke test below derives its list from
          # exactly that -- so a bin whose `required-features` are not in its
          # crate's `default` is reported here but skipped by a plain
          # `--workspace` build. Cargo does not warn about the skip, it simply
          # produces no binary, and the smoke test then dies on a path that does
          # not exist (exit 127).
          #
          # Building all features keeps the invariant the smoke test assumes --
          # "every bin cargo metadata reports has been built" -- true by
          # construction. This workspace declares no features at all today, so
          # the flag changes nothing now; it is here so that adding a
          # feature-gated binary later cannot reintroduce the mismatch.
          #
          # `--locked` still holds alongside it: enabling a feature can only
          # activate optional dependencies, which Cargo.lock already pins.
          echo "::group::cargo build --release --all-features"
          cargo build --release --locked --workspace --all-features
          echo "::endgroup::"

      # The gates above prove the code is correct; this proves the artefacts run.
      # `profile.release` sets `panic = "abort"`, `lto = "fat"` and `opt-level =
      # "z"`, none of which the test profile exercises -- so a release binary can
      # fail to start while every test passes.
      #
      # The binary list is DISCOVERED from cargo metadata rather than written out
      # here. An earlier version named a single binary as a literal string and
      # so survived a project rename untouched, then broke on the next run: a
      # stale name in a shell string is invisible to cargo, clippy and rustfmt,
      # which is why nothing caught it. Deriving the list means adding or
      # renaming a binary can never leave this step behind again -- a property
      # this repository has since exercised, renaming its one crate with no edit
      # needed here. `select(.publish != [])` drops `publish = false` members:
      # those are dev-only harnesses, not release artefacts.
      - name: Smoke-test the release binaries
        shell: bash -l {0}
        run: |
          set -eo pipefail

          # Assigned FIRST, then queried -- not read straight from a process
          # substitution. `mapfile -t ARR < <(cmd)` discards cmd's exit status
          # entirely, so a failing `cargo metadata` would surface as an empty
          # list, which the library-workspace branch below would then report as
          # a clean skip. Under `set -e` a failed command substitution in an
          # assignment does abort. One invocation also serves both queries.
          METADATA="$(cargo metadata --format-version 1 --no-deps)"

          # Every bin target, BEFORE the publish filter. The two reasons the
          # publishable list can come back empty need telling apart, and only
          # this unfiltered count separates them.
          mapfile -t ALL_BINS < <(printf '%s' "${METADATA}" \
            | jq -r '.packages[].targets[]
                     | select(.kind[] == "bin") | .name' | sort -u)

          mapfile -t BINS < <(printf '%s' "${METADATA}" \
            | jq -r '.packages[] | select(.publish != []) | .targets[]
                     | select(.kind[] == "bin") | .name' | sort -u)

          # A workspace with no bin targets AT ALL is a library, and a library
          # has nothing to smoke-test. This one is: `rustup-toolchain-tests`
          # ships a single lib plus its build script. The unconditional `exit 1`
          # that used to stand here came from the workspace this `.github/` was
          # transplanted from, which always shipped a binary and where an empty
          # list really did mean something had broken. Carried over unchanged,
          # it failed this repository on its correct and permanent state.
          if [ "${#ALL_BINS[@]}" -eq 0 ]; then
            echo "::notice::no bin targets in this workspace; nothing to smoke-test"
            exit 0
          fi

          # Bins exist but every one is `publish = false`. That is NOT a library
          # workspace, it is a metadata mistake -- a release artefact silently
          # demoted -- so the original fail-closed behaviour is kept for exactly
          # this case, which is what it was always guarding against.
          if [ "${#BINS[@]}" -eq 0 ]; then
            echo "::error::this workspace declares bin targets (${ALL_BINS[*]}) but none are publishable; a release artefact has probably been marked publish = false by mistake"
            exit 1
          fi

          # `--help` with no per-binary special-casing: clap answers it at the
          # top level before any subcommand dispatch runs, so a binary that
          # would otherwise block on stdin still returns promptly.
          for BIN in "${BINS[@]}"; do
            echo "::group::${BIN}"

            # Checked explicitly so a missing binary names its own cause. Without
            # this the invocation below runs a path that does not exist and bash
            # exits 127 with nothing but "No such file or directory", which reads
            # like a broken runner rather than a build/metadata disagreement --
            # the real cause is a bin whose `required-features` the build step
            # did not enable.
            if [ ! -x "./target/release/${BIN}" ]; then
              echo "::error::${BIN} is a bin target in cargo metadata but was not built. If it declares required-features, the build step must enable them (it uses --all-features)."
              exit 1
            fi

            "./target/release/${BIN}" --help

            echo "::endgroup::"
          done

      - name: Assert no changes on git tree
        shell: bash -l {0}
        run: |
          set -eo pipefail

          echo "::group::Git Diff"
          if ! git diff --exit-code --quiet ; then
            echo "::error::Changes are detected, please check the diff below"
            git diff .
            exit 1
          fi
          echo "::endgroup::"

  # # ──────────────────────────────────────────────
  # #  8.  Release
  # # ──────────────────────────────────────────────
  # release:
  #   name: Release
  #   # `msrv` is named here, not just left to run alongside, because this job is
  #   # the last point before crates reach crates.io. A published crate whose
  #   # `rust-version` is a lie is the failure the MSRV gate exists to prevent, and
  #   # a gate that cannot block the release does not prevent anything.
  #   needs: [tests, build, version, msrv]
  #   if: >
  #     success()
  #     && needs.tests.result == 'success'
  #     && needs.build.result == 'success'
  #     && needs.version.result == 'success'
  #     && needs.msrv.result == 'success'
  #     && github.ref_name == github.event.repository.default_branch
  #     && github.event_name == 'push'
  #   runs-on: ${{ vars.RUNS_ON_ARM64 || vars.RUNS_ON || 'ubuntu-latest' }}
  #   permissions:
  #     contents: write
  #     pull-requests: write
  #   outputs:
  #     # The version GitVersion computed and the Tag step actually pushed, handed
  #     # to the publish job below rather than re-derived there. publish.yml falls
  #     # back to stripping a leading `v` off `github.ref_name`, which is correct
  #     # only when a tag ref triggered it: under `workflow_call` that expression
  #     # resolves to the CALLER's ref, so a push to main produced the literal
  #     # version string `main`. Passing the value keeps one source of truth.
  #     version: ${{ needs.version.outputs.version }}

  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
  #       with:
  #         submodules: "recursive"
  #         fetch-depth: 0
  #         fetch-tags: "true"
  #         ref: ${{ env.REF }}

  #     - name: Setup Environment
  #       uses: ./.github/actions/setup-rust-env

  #     # No GitVersion steps here. The `version` job above ran it once; this job
  #     # reads `needs.version.outputs.*`. Provisioning the .NET tool a second time
  #     # cost minutes and, worse, computed the version twice -- two answers that
  #     # could disagree if a tag landed between them.
  #     - name: Determine Working Branch
  #       id: working-branch
  #       shell: bash
  #       run: |
  #         set -eo pipefail
  #         if [[ "${{ github.ref_name }}" == "${{ github.event.repository.default_branch }}" ]]; then
  #           WORKING_BRANCH="release/v${{ needs.version.outputs.version }}"
  #         else
  #           WORKING_BRANCH="prerelease/v${{ needs.version.outputs.version }}"
  #         fi
  #         echo "branch=${WORKING_BRANCH}" >> "$GITHUB_OUTPUT"
  #         echo "Working Branch: ${WORKING_BRANCH}"

  #     - name: Skip prerelease versions
  #       if: needs.version.outputs.preReleaseLabel != ''
  #       run: |
  #         echo "Skipping release for prerelease version: ${{ needs.version.outputs.version }}"
  #         exit 0

  #     - name: Update Workflow Files With New Version
  #       id: update-workflows
  #       shell: bash
  #       env:
  #         GH_TOKEN: ${{ secrets.GITHUB_TOKEN || github.token }}
  #       run: |
  #         set -eo pipefail

  #         echo "Creating/Updating Working branch: ${{ steps.working-branch.outputs.branch }}"
  #         git checkout -B "${{ steps.working-branch.outputs.branch }}"

  #         echo "Updating Workflow files to version v${{ needs.version.outputs.version }}"

  #         REPO_OWNER="${{ github.repository_owner }}"
  #         REPO_NAME="${{ github.event.repository.name }}"
  #         NEW_VERSION="v${{ needs.version.outputs.version }}"

  #         find .github -type f \( -name "*.yml" -o -name "*.yaml" \) ! -name "_release.yml" \
  #           | while read -r file; do
  #           sed -i -E "s#(${REPO_OWNER}/${REPO_NAME}/.+)@(main|v[0-9]+(\.[0-9]+)*(-[a-zA-Z0-9.]+)?)#\1@${NEW_VERSION}#g" "$file"
  #         done

  #         git add .github
  #         git commit -m "chore: Update action references to v${{ needs.version.outputs.version }} [skip ci]" \
  #           || echo "No changes to commit"
  #         git push -u origin "${{ steps.working-branch.outputs.branch }}" --force

  #     - name: Tag
  #       id: tag
  #       shell: bash -l {0}
  #       env:
  #         GH_TOKEN: ${{ secrets.GITHUB_TOKEN || github.token }}
  #       run: |
  #         echo "::group::Tag Information"
  #         echo "::group::Tagger Function"
  #         function tagger() {
  #           VERSION="$1"
  #           echo "Releasing Version: ${VERSION}"
  #           TAG_EXISTS=$(git ls-remote -q --tags | grep -q "refs/tags/${VERSION}$" && echo true || echo false)
  #           if [[ "${TAG_EXISTS}" = true ]];
  #           then
  #             HAS_RELEASE=$(gh release list --json tagName -q '.[].tagName' | grep -E "^(${VERSION})$" && echo true || echo false)
  #             if [[ "${HAS_RELEASE}" == true ]];
  #             then
  #               gh release delete --yes "${VERSION}"
  #             fi
  #             git push origin --delete "${VERSION}"
  #             git tag -d "${VERSION}"
  #           fi

  #           echo "::group::Tagging Version ${VERSION}"
  #           git tag -a "${VERSION}" -m "chore: Release Version ${VERSION}"
  #           git push -f --tags --follow-tags
  #           # NOTE: no `git push --set-upstream origin <ref>` here. On
  #           # pull_request events github.ref_name is the virtual merge
  #           # ref like 9/merge, which isn't a pushable branch — push
  #           # fails with "src refspec 9/merge does not match any" and
  #           # leaks an error into the CI log. Tags are already pushed
  #           # above via --follow-tags; set-upstream was redundant on
  #           # main pushes and broken on PR events.
  #           echo "::endgroup::"
  #         }
  #         echo "::endgroup::"

  #         echo "::group::Tagging Version v${{ needs.version.outputs.semVer }}"
  #         tagger "v${{ needs.version.outputs.semVer }}"
  #         echo "::endgroup::"

  #         if [[ "${{ github.ref_name  }}" == "${{ github.event.repository.default_branch }}" && "${{ github.event_name }}" != "pull_request" ]];
  #         then
  #           echo "::group::Tagging Version v${{ needs.version.outputs.majorMinorPatch }}"
  #           tagger "v${{ needs.version.outputs.majorMinorPatch }}"
  #           echo "::endgroup::"

  #           echo "::group::Tagging Version v${{ needs.version.outputs.major }}.${{ needs.version.outputs.minor }}"
  #           tagger "v${{ needs.version.outputs.major }}.${{ needs.version.outputs.minor }}"
  #           echo "::endgroup::"

  #           echo "::group::Tagging Version v${{ needs.version.outputs.major }}"
  #           tagger "v${{ needs.version.outputs.major }}"
  #           echo "::endgroup::"
  #         fi
  #         echo "::endgroup::"

  #     - name: Find Associated PR
  #       id: find-pr
  #       if: ${{ github.ref_name != github.event.repository.default_branch }}
  #       shell: bash
  #       env:
  #         GH_TOKEN: ${{ secrets.GITHUB_TOKEN || github.token }}
  #       run: |
  #         set -eo pipefail
  #         PR_NUMBER=""
  #         if [[ "${{ github.event_name }}" == "pull_request" ]]; then
  #           PR_NUMBER="${{ github.event.pull_request.number }}"
  #         else
  #           PR_NUMBER=$(gh pr list \
  #             --head "${{ github.ref_name }}" \
  #             --state open \
  #             --json number \
  #             --jq '.[0].number // empty')
  #         fi
  #         echo "number=${PR_NUMBER}" >> "$GITHUB_OUTPUT"

  #     - name: Add PR Comment With Tag Link
  #       if: ${{ steps.find-pr.outputs.number != '' }}
  #       uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
  #       with:
  #         github-token: ${{ secrets.GITHUB_TOKEN || github.token }}
  #         script: |
  #           const version = `v${{ needs.version.outputs.version }}`;
  #           const semVer = `v${{ needs.version.outputs.semVer }}`;
  #           const major = `${{ needs.version.outputs.major }}`;
  #           const minor = `${{ needs.version.outputs.minor }}`;
  #           const patch = `${{ needs.version.outputs.patch }}`;
  #           const shortSha = `${{ needs.version.outputs.shortSha }}`;
  #           const branch = `${{ steps.working-branch.outputs.branch }}`;
  #           const tagUrl = `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/tree/${version}`;

  #           const actor = `${{ github.actor }}`;
  #           const actorUrl = `${context.serverUrl}/${actor}`;
  #           const sender = `${{ github.event.pull_request.head.user.login || github.event.sender.login }}`;
  #           const senderUrl = `${context.serverUrl}/${sender}`;

  #           const body = [
  #             `## 🏷️ Pre-release Tag Created`,
  #             ``,
  #             `| | |`,
  #             `|---|---|`,
  #             `| **Tag** | [\`${semVer}\`](${tagUrl}) |`,
  #             `| **Version** | \`${major}.${minor}.${patch}\` |`,
  #             `| **Branch** | \`${branch}\` |`,
  #             `| **Commit** | \`${shortSha}\` |`,
  #             `| **Actor** | [@${actor}](${actorUrl}) |`,
  #             `| **Author** | [@${sender}](${senderUrl}) |`,
  #             ``,
  #             `> 📦 To test the Rust Toolchain action from this PR:`,
  #             `> \`\`\`yaml`,
  #             `> - uses: ${context.repo.owner}/${context.repo.repo}@${semVer}`,
  #             `> \`\`\``,
  #           ].join('\n');

  #           const issueNumber = ${{ steps.find-pr.outputs.number }};
  #           const { data: comments } = await github.rest.issues.listComments({
  #             issue_number: issueNumber,
  #             owner: context.repo.owner,
  #             repo: context.repo.repo,
  #           });

  #           const existing = comments
  #             .filter(c => c.user?.type === 'Bot'
  #               && c.body?.includes('## 🏷️ Pre-release Tag Created'))
  #             .sort((a, b) => new Date(b.created_at) - new Date(a.created_at));

  #           for (const dup of existing.slice(1)) {
  #             await github.rest.issues.deleteComment({
  #               comment_id: dup.id,
  #               owner: context.repo.owner,
  #               repo: context.repo.repo,
  #             });
  #           }

  #           if (existing.length > 0) {
  #             await github.rest.issues.updateComment({
  #               comment_id: existing[0].id,
  #               owner: context.repo.owner,
  #               repo: context.repo.repo,
  #               body
  #             });
  #           } else {
  #             await github.rest.issues.createComment({
  #               issue_number: issueNumber,
  #               owner: context.repo.owner,
  #               repo: context.repo.repo,
  #               body
  #             });
  #           }

  #     - name: Generate release notes
  #       if: ${{ github.ref_name == github.event.repository.default_branch }}
  #       shell: bash
  #       run: |
  #         set -Eeuo pipefail

  #         # Build the release body from the conventional-commit history with the
  #         # SAME renderer that writes CHANGELOG.md, rather than `gh
  #         # --generate-notes`. Those produced different documents for the same
  #         # release: `--generate-notes` lists merged PRs with no awareness of
  #         # cliff.toml's commit-type grouping, so the release page and the
  #         # changelog disagreed on both content and shape.
  #         #
  #         # `--latest` is the range selector that limits output to ONE release
  #         # section. Without it the entire history renders into the body, and
  #         # `--tag` alone only *labels* unreleased commits, it does not filter.
  #         # The `Tag` step earlier in this job already pushed vX.Y.Z, and
  #         # cliff.toml's `tag_pattern` matches only full versions (the floating
  #         # v<major> / v<major>.<minor> tags are excluded), so `--latest`
  #         # resolves unambiguously to this release.
  #         #
  #         # `--strip header` drops the changelog's global header so only this
  #         # release's grouped sections and the compare-link footer land here.
  #         mise run changelog --latest --strip header --output RELEASE_NOTES.md

  #     - name: Create GitHub Release
  #       if: ${{ github.ref_name == github.event.repository.default_branch }}
  #       shell: bash
  #       env:
  #         GH_TOKEN: ${{ secrets.GITHUB_TOKEN || github.token }}
  #       run: |
  #         set -eo pipefail

  #         # Clean up draft releases
  #         gh release list \
  #           --json tagName,isDraft \
  #           -q '.[] | select(.isDraft) | .tagName' \
  #           | while read -r draft; do
  #           echo "Removing Draft Release: ${draft}"
  #           gh release delete --yes "${draft}"
  #         done

  #         # The generate step above always writes RELEASE_NOTES.md on success,
  #         # so missing-or-empty here means it was skipped or failed. Bail now
  #         # rather than publish a release with an empty body -- `gh release
  #         # create` accepts an empty notes file without complaint.
  #         if [ ! -s RELEASE_NOTES.md ]; then
  #           echo "::error::RELEASE_NOTES.md is missing or empty. Did the 'Generate release notes' step run?"
  #           exit 1
  #         fi

  #         # Create release targeting the working branch
  #         gh release create "v${{ needs.version.outputs.version }}" \
  #           --target "${{ steps.working-branch.outputs.branch }}" \
  #           --notes-file RELEASE_NOTES.md \
  #           --verify-tag \
  #           --latest

  # # ──────────────────────────────────────────────
  # #  9.  Publish — package, attest, sign, publish
  # # ──────────────────────────────────────────────
  # # Invoked rather than triggered. `release` above pushes the tag with
  # # GITHUB_TOKEN, and GitHub does not raise workflow-triggering events for that
  # # token, so publish.yml's `push: tags` never fired and nothing was ever
  # # packaged, attested, signed or published. Calling it gives real `needs:`
  # # ordering across files without a PAT.
  # #
  # # Permissions are the union of what the called workflow's jobs declare; a
  # # reusable workflow cannot hold more than its caller grants.
  # publish:
  #   name: Publish
  #   needs: [release]
  #   if: ${{ success() && needs.release.result == 'success' && github.ref_name == github.event.repository.default_branch }}
  #   permissions:
  #     actions: read
  #     contents: write
  #     id-token: write
  #     pages: write
  #   uses: ./.github/workflows/publish.yml
  #   with:
  #     version: ${{ needs.release.outputs.version }}
  #     # EXPLICIT, because `dry-run` defaults to `true` in publish.yml and that
  #     # default is deliberate -- the gate there fails closed, so it publishes only
  #     # on an explicit "false" and any other value (including the empty string a
  #     # trigger without inputs produces) is treated as a dry run.
  #     #
  #     # Omitting this key therefore did not mean "use the caller's intent", it
  #     # meant "never publish": the whole release path ran green while uploading
  #     # nothing. This job is already reachable only from a push to the default
  #     # branch (see the `release` job it needs), which is exactly the condition
  #     # under which a release SHOULD reach crates.io.
  #     dry-run: ${{ inputs.dry-run || false }}
  #   secrets: inherit
